console.log('oi');

// Tipando variáveis
let nome: string
let idade: number
let maiorIdade: boolean

// Atribuindo valores
nome = 'Ryan'
idade = 18
maiorIdade = true

// Tipando ARRAY
let lista: string[]
lista = ['Ryan']

let pessoa: [string, number, boolean]
pessoa = [nome, idade, maiorIdade]

// Tipando Objetos

let objeto: object
objeto = {
    nome: 'Ryan',
    idade: 18
}